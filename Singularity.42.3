BootStrap: docker
From: opensuse:42.3

%runscript
    python "$@"

%post
    zypper -n install libpsm_infinipath1
    zypper ar https://download.opensuse.org/repositories/home:/pelupes:/amuse/openSUSE_Leap_42.3/ amuse
    zypper --gpg-auto-import-keys refresh amuse
    echo "install amuse..."
    zypper -n install amuse
    sed -i 's/from .backend_nbagg import TimerTornado/from timertornado import TimerTornado/' /usr/lib64/python2.7/site-packages/matplotlib/backends/backend_webagg.py
